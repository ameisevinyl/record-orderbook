{
  "name": "record-orderbook",
  "version": "0.1.0",
  "description": "Self-contained browser tools for vinyl record production (cutting orders, labels, covers, shipping).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node build/build.js"
  }
}
